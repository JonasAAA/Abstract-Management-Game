namespace Game1
{
    public sealed class SimpleHistoricProporSplitter<TKey>
        where TKey : notnull
    {
        public Dictionary<TKey, ulong> Split(ulong amount, Dictionary<TKey, UDouble> importances)
        {
            // Implement this, also think what API would work for when star distributes radiant energy and some of it goes to vacuum
            throw new NotImplementedException();
        }
    }
}
